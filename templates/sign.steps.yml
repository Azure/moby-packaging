parameters:
  folderPath: ''

steps:
- template: sign.steps.windows.yml
  parameters:
    folderPath: ${{ parameters.folderPath }}
    pattern: '*.exe'
- template: sign.steps.linux.yml
  parameters:
    folderPath: ${{ parameters.folderPath }}
    pattern: '*'
