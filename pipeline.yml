name: INPUT_DIR

variables:
  amd.pool: staging-pool-amd64-mariner-2

stages:
  - stage: INPUT_DIR
    jobs:
      - job: INPUT_DIR
        pool: $(amd.pool)
        timeoutInMinutes: 5
        steps:
          - bash: |
              set -exu

              echo "$INPUT_DIR"
            displayName: Validate Input Data Format
            env:
              INPUT_DIR: "hello, world!"
